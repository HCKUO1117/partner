import 'package:flutter/material.dart';

class ArticleListTitle extends StatelessWidget {
  const ArticleListTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
