import 'package:flutter/material.dart';

class AccountInformationLayout extends StatelessWidget {
  const AccountInformationLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
